# Application Template - Spring-Boot

## Usage

* `mvn test` to run the tests
* `mvn spring-boot:run` to start the application

## REST API

### GET /

#### Description

Returns the "Hello World" string.

#### Responses

* 200: The string

  ```
Hello World
  ```
